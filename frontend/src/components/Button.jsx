const Button = ({ text }) => {
  return (
    <div className="flex items-center justify-center ">
      <Button className="text-white bg-gray-800 px-5 py-3">{text}</Button>
    </div>
  )
}
export default Button